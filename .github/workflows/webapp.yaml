on:
  push:
    braches:
      - '*'
    paths:
      - 'docker/webapp/**'

jobs:
  build:
    name: build
    runs-on: ubuntu-18.04

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Build Image
        run: docker build -t webapp ./docker/webapp
